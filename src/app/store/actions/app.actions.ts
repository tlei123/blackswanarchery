import { createAction, props } from '@ngrx/store';

export const closeSplashVideo = createAction('[App] Close splash video');

export const endSplashVideo = createAction('[App] End splash video');

export const skipSplashVideo = createAction('[App] Skip splash video');

export const resetSplashVideo = createAction('[App] Reset splash video');
