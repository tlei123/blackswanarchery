import { createAction, props } from '@ngrx/store';

export const close = createAction('[SplashVideo] Close');

export const end = createAction('[SplashVideo] End');

export const skip = createAction('[SplashVideo] Skip');

export const reset = createAction('[SplashVideo] Reset');
